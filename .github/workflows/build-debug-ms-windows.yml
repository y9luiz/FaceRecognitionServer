name: Build MS Windows

on: [pull_request,workflow_dispatch]

on:
    push:
      branches: [ "master" ]

jobs:

  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build on Windows
      run:  PowerShell ./scripts/build_windows.ps1
